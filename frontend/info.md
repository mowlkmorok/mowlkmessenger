# How to use:
  
  - Create your flutter project 
  - Copy essencial code and make change
  - Change the *websocket server* in `chatScreen.dart`

```dart
 String uri = 'ws://localhost:8080/table/' + args['table'];
```
